// Tennessee and its eight bordering states — the geographic scope COMPASS is built for.
// Views that would otherwise show all 50 states + territories narrow to this set so the
// Tennessee signal isn't buried in national noise.

export const TN_REGION_NAMES = new Set([
  "Tennessee", "Kentucky", "Virginia", "North Carolina", "Georgia",
  "Alabama", "Mississippi", "Arkansas", "Missouri",
]);

export const TN_REGION_ABBREVS = new Set([
  "TN", "KY", "VA", "NC", "GA", "AL", "MS", "AR", "MO",
]);
